export namespace Contact {
  export type Attributes = {
    id?: string;
    entityId: string;
    userId: string;
    name: string;
    email?: string;
    phone?: string;
    createdAt?: string;
    updatedAt?: string;
  };
}
